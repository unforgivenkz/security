<% provide(:title, @user.name) %>
<% provide(:img, "follow1.jpg") %>
<% provide(:h1, @title) %>
<div class="showus">
      <%= image_tag(@user.avatar_url) if @user.avatar? %> <!--загруженая картинка, если присутствует-->
      <%= image_tag(img_title("personal.png")) if !@user.avatar?%>
      <h3><%= @user.name %></h3>
      <%= link_to "Вопросы: #{@user.microposts.count}", @user %>
      <%= render 'shared/stats' %>
      <% if @users.any? %>
        <div class="user_avatars">
          <% @users.each do |user| %>
            <%= link_to image_tag(user.avatar.thumb.url), user %>
          <% end %>
        </div>  
      <% end %>
</div>
  <div class="showlen">
    <% if @users.any? %>
      <ul class="users">
        <%= render @users %>
      </ul>
      <%= will_paginate %>
    <% end %>
  </div>