<% provide(:title, "Все пользователи") %>
<% provide(:h1, "Все пользователи") %>
<% provide(:img, "reg.jpg") %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag(img_title("personal.png")) %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>