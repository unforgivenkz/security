

<li id="<%= feed_item.id %>" class = "hov">


<% if current_user?(feed_item.user) %>
  <span class = "imgicon"><%= link_to image_tag(img_title("close.png"), :title => "Удалить") , feed_item, method: :delete,
                                     data: { confirm: "Вы уверены?" },
                                     title: feed_item.content %></span>
<% end %>


	<%= link_to image_tag(feed_item.user.avatar.thumb.url), feed_item.user if feed_item.user.avatar? %>
	<%= link_to image_tag(img_title("personalmini.png")), feed_item.user if !feed_item.user.avatar?%>
    <%= link_to feed_item.user.name, feed_item.user %>

    
  <span class="contentmic" ><%= link_to feed_item.content.slice(0...120),feed_item  %><%= "..." if feed_item.content.size>120%></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(feed_item.created_at) %> ago.
  </span>
                            


</li>


